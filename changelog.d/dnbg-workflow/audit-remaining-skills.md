Audited the skills the recent trimming passes had not covered. Two changes:
`reviewer-setup` states its installation-permissions check once instead of twice,
and `work-summary` drops Slack composer trivia that dated itself in favour of the
part a user can act on. The other four files were read and left alone.
