`reviewer`'s issue-scoped mode moves to `issue-mode.md` alongside the skill,
leaving a pointer in `SKILL.md`. It was 2,124 words — 29% of the skill — that a
PR-scoped review loaded and never used. Behaviour is unchanged; a review assigned
to an issue reads the sibling file first.
