{
  "_comment": [
    "The permissions a reviewer needs, declared once. bootstrap.py builds a new",
    "App's manifest from it, and mint-token.sh checks every token it mints",
    "against it.",
    "",
    "A MINIMUM, not an exact set. The App may be installed for other purposes and",
    "hold permissions beyond these; that is the owner's business and not a fault.",
    "Only a grant that is absent or too weak is reported, since only that costs a",
    "capability."
  ],
  "required": {
    "pull_requests": "write",
    "contents": "write",
    "checks": "read",
    "actions": "read",
    "statuses": "read",
    "metadata": "read"
  },
  "why": {
    "pull_requests": "submit reviews, inline comments, thread replies",
    "contents": "read the diff; resolve threads; have the verdict counted at all",
    "checks": "check runs",
    "actions": "checkSuite.workflowRun, which the rollup dereferences",
    "statuses": "combined commit status, for CI that posts statuses instead of check runs",
    "metadata": "mandatory"
  },
  "consequence": {
    "pull_requests": "reviews and inline comments cannot be posted",
    "contents": "reviews post but do not count — GitHub leaves a review by an App without this out of latestOpinionatedReviews, so reviewDecision never moves and the merge box ignores the verdict. Resolving a thread also fails.",
    "checks": "check runs are invisible, so a red build reads as no build",
    "actions": "the rollup cannot dereference workflow runs",
    "statuses": "CI that posts commit statuses instead of check runs is invisible",
    "metadata": "nothing works"
  }
}
